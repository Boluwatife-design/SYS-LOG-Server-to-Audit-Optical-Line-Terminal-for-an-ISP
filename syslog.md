# Managing via SSH
- How it works: Once the VM has a working network IP, you open the standard Terminal or Command Prompt directly on your personal laptop and type: ssh username@IP

## Verify Your Current Network Setup

      ip a
- Check if the server can see its own gateway

      ip route
